#cloud-config
packages:
  - telnet
runcmd:
  - aws ec2 associate-address --instance-id $(curl http://169.254.169.254/latest/meta-data/instance-id) --allocation-id ${bastion_elasticip_id} --allow-reassociation --region ${region_eip}